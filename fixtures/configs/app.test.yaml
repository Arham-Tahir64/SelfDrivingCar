runtime:
  backend: stub
  tick_hz: 20
  max_ticks: 2
  output_dir: outputs/tests
  log_level: INFO
  record_replay: true
  enable_visualization: false
  weather_preset: ClearNoon
carla:
  host: 127.0.0.1
  port: 2000
  town: Town04
evaluation:
  latency_budget_ms:
    perception: 60
    localization: 5

